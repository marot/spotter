---
---

<section class="hero">
  <div class="hero-grid">
    <div class="hero-left">
      <div class="hero-badge">
        <span class="hero-badge-dot"></span>
        Open source &middot; MIT License
      </div>

      <h1 class="hero-headline">
        Know what your AI agents <span class="text-gradient">actually changed.</span>
      </h1>

      <p class="hero-sub">
        Spotter visualizes Claude Code transcripts, links commits to conversations,
        and helps you stay on top of every change &mdash; even across 4+ parallel agents.
      </p>

      <div class="copy-command" data-command="git clone https://github.com/marot/spotter && cd spotter && mix setup && mix phx.server">
        <svg class="copy-terminal-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="2" y="3" width="20" height="14" rx="2"/><path d="M6 8l4 3-4 3"/><line x1="12" y1="14" x2="18" y2="14"/></svg>
        <span class="copy-prompt">$</span>
        <code class="copy-text">git clone https://github.com/marot/spotter && cd spotter && mix setup && mix phx.server</code>
        <button class="copy-btn" type="button" aria-label="Copy to clipboard">
          <svg class="icon-copy" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 01-2-2V4a2 2 0 012-2h9a2 2 0 012 2v1"/></svg>
          <svg class="icon-check" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg>
        </button>
      </div>

      <a href="https://github.com/marot/spotter" class="hero-github" target="_blank" rel="noopener noreferrer">
        View on GitHub
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="14" height="14"><path d="M18 13v6a2 2 0 01-2 2H5a2 2 0 01-2-2V8a2 2 0 012-2h6"/><polyline points="15 3 21 3 21 9"/><line x1="10" y1="14" x2="21" y2="3"/></svg>
      </a>
    </div>

    <div class="hero-right">
      <div class="screenshot-placeholder">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" width="32" height="32"><rect x="2" y="3" width="20" height="14" rx="2"/><path d="M8 21h8"/><path d="M12 17v4"/></svg>
        <span>Screenshot: Spotter Dashboard</span>
      </div>
    </div>
  </div>
</section>

<script>
  document.querySelectorAll('.copy-command').forEach((el) => {
    el.addEventListener('click', () => {
      const command = el.getAttribute('data-command');
      if (!command) return;
      navigator.clipboard.writeText(command).then(() => {
        el.classList.add('copied');
        setTimeout(() => el.classList.remove('copied'), 2000);
      });
    });
  });
</script>

<style>
  .hero {
    padding: 6rem 0 5rem;
    animation: heroIn 520ms cubic-bezier(0.22, 1, 0.36, 1) both;
  }

  @keyframes heroIn {
    from { opacity: 0; transform: translateY(24px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .hero-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .hero-left {
    text-align: left;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    font-weight: 500;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--accent);
    border: 1px solid var(--border);
    border-radius: 100px;
    padding: 0.35rem 1rem;
    margin-bottom: 2rem;
    background: rgba(212, 145, 94, 0.04);
  }

  .hero-badge-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--accent);
  }

  .hero-headline {
    font-size: clamp(2rem, 5.5vw, 3.5rem);
    font-weight: 700;
    line-height: 1.15;
    letter-spacing: -0.025em;
    color: #f0f3f8;
    margin-bottom: 1.5rem;
  }

  .text-gradient {
    background: linear-gradient(135deg, var(--accent), var(--accent-strong));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-sub {
    font-size: clamp(1rem, 2vw, 1.15rem);
    color: var(--muted);
    max-width: 32rem;
    line-height: 1.7;
    margin-bottom: 2rem;
  }

  .copy-command {
    cursor: pointer;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 0.9rem 1.25rem;
    font-family: var(--font-mono);
    font-size: 0.85rem;
    max-width: 36rem;
    transition: border-color 0.2s;
  }

  .copy-command:hover {
    border-color: rgba(212, 145, 94, 0.4);
  }

  .copy-terminal-icon {
    width: 1rem;
    height: 1rem;
    color: var(--accent);
    flex-shrink: 0;
  }

  .copy-prompt {
    color: rgba(212, 145, 94, 0.5);
    flex-shrink: 0;
  }

  .copy-text {
    color: var(--text);
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    flex: 1;
    min-width: 0;
  }

  .copy-btn {
    background: none;
    border: none;
    cursor: pointer;
    padding: 0;
    margin-left: auto;
    flex-shrink: 0;
    display: flex;
  }

  .copy-btn svg {
    width: 1rem;
    height: 1rem;
    color: var(--muted);
    transition: color 0.15s;
  }

  .copy-command:hover .copy-btn .icon-copy {
    color: var(--accent);
  }

  .icon-check {
    display: none;
    color: #4ade80;
  }

  .copy-command.copied .icon-copy {
    display: none;
  }

  .copy-command.copied .icon-check {
    display: block;
    color: #4ade80;
  }

  .hero-github {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    margin-top: 1.25rem;
    font-size: 0.85rem;
    color: var(--muted);
    text-decoration: none;
    transition: color 0.15s;
  }

  .hero-github:hover {
    color: var(--accent);
  }

  .hero-right {
    transform: rotate(1.5deg);
  }

  .screenshot-placeholder {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    aspect-ratio: 16 / 10;
    border: 1px dashed rgba(212, 145, 94, 0.3);
    border-radius: 12px;
    background: linear-gradient(135deg, rgba(212, 145, 94, 0.03), rgba(212, 145, 94, 0.01));
    color: var(--muted);
    font-size: 0.75rem;
    font-family: var(--font-mono);
    opacity: 0.6;
  }

  @media (max-width: 768px) {
    .hero-grid {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }

    .hero-right {
      transform: none;
    }
  }

  @media (max-width: 480px) {
    .hero {
      padding: 3.5rem 0 3rem;
    }

    .hero-headline {
      font-size: 1.75rem;
    }
  }
</style>
