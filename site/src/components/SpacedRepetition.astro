---
---

<section class="spaced-repetition">
  <div class="sr-grid">
    <!-- Left: Timeline -->
    <div class="sr-left">
      <div class="sr-timeline">
        <div class="sr-timeline-line"></div>

        <div class="sr-day-group" style="opacity: 1">
          <div class="sr-dot"></div>
          <div class="sr-day">Today</div>
          <div class="sr-items">
            <div class="sr-item">Auth module refactor</div>
            <div class="sr-item">API rate limiting</div>
          </div>
        </div>

        <div class="sr-day-group" style="opacity: 0.8">
          <div class="sr-dot"></div>
          <div class="sr-day">Tomorrow</div>
          <div class="sr-items">
            <div class="sr-item">Database migration v12</div>
          </div>
        </div>

        <div class="sr-day-group" style="opacity: 0.6">
          <div class="sr-dot"></div>
          <div class="sr-day">In 3 days</div>
          <div class="sr-items">
            <div class="sr-item">Webhook retry logic</div>
          </div>
        </div>

        <div class="sr-day-group" style="opacity: 0.4">
          <div class="sr-dot"></div>
          <div class="sr-day">Next week</div>
          <div class="sr-items">
            <div class="sr-item">Cache invalidation</div>
            <div class="sr-item">Error boundaries</div>
          </div>
        </div>
      </div>

      <div class="sr-screenshot">
        <div class="screenshot-placeholder">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" width="32" height="32"><rect x="2" y="3" width="20" height="14" rx="2"/><path d="M8 21h8"/><path d="M12 17v4"/></svg>
          <span>Screenshot: Spaced Repetition Dashboard</span>
        </div>
      </div>
    </div>

    <!-- Right: Copy -->
    <div class="sr-right">
      <h2 class="section-label">Spaced Repetition</h2>
      <p class="sr-title">
        Stay on top of everything.
        <span class="text-gradient">Even across projects.</span>
      </p>
      <p class="sr-body">
        When you're running multiple agents across multiple projects, it's impossible to
        remember everything that was changed. Code gets merged, reviewed once, and forgotten.
      </p>
      <p class="sr-body">
        Spotter's spaced repetition system surfaces past changes on a schedule &mdash; the same
        technique used for learning languages, applied to code review. Changes you've seen
        recently fade to the back. Changes you haven't reviewed in a while come forward.
      </p>
      <div class="sr-badges">
        <div class="sr-badge">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="16" height="16"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
          <span>Scheduled review cards</span>
        </div>
        <div class="sr-badge">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="16" height="16"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
          <span>Adaptive intervals</span>
        </div>
        <div class="sr-badge">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="16" height="16"><path d="M12 2a7 7 0 017 7c0 2.38-1.19 4.47-3 5.74V17a2 2 0 01-2 2h-4a2 2 0 01-2-2v-2.26C6.19 13.47 5 11.38 5 9a7 7 0 017-7z"/><line x1="9" y1="21" x2="15" y2="21"/></svg>
          <span>Cross-project tracking</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .spaced-repetition {
    padding: 6rem 0;
    border-top: 1px solid var(--border);
  }

  .sr-grid {
    display: grid;
    grid-template-columns: 5fr 7fr;
    gap: 4rem;
  }

  /* Timeline */
  .sr-timeline {
    position: relative;
    padding-left: 1.5rem;
  }

  .sr-timeline-line {
    position: absolute;
    left: 0;
    top: 0.5rem;
    bottom: 0;
    width: 1px;
    background: linear-gradient(to bottom, rgba(212, 145, 94, 0.4), rgba(212, 145, 94, 0.05));
  }

  .sr-day-group {
    margin-bottom: 1.25rem;
    position: relative;
  }

  .sr-dot {
    position: absolute;
    left: -1.5rem;
    top: 0.35rem;
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--accent);
    transform: translateX(-50%);
  }

  .sr-day {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    font-weight: 500;
    color: var(--accent);
    margin-bottom: 0.4rem;
  }

  .sr-items {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  .sr-item {
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 0.5rem 0.75rem;
    font-size: 0.85rem;
    color: var(--text);
  }

  .sr-screenshot {
    margin-top: 2rem;
  }

  /* Right: Copy */
  .section-label {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    font-weight: 500;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: 1rem;
  }

  .sr-title {
    font-size: clamp(1.5rem, 3.5vw, 2.25rem);
    font-weight: 700;
    color: #f0f3f8;
    margin-bottom: 1.5rem;
  }

  .text-gradient {
    background: linear-gradient(135deg, var(--accent), var(--accent-strong));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .sr-body {
    font-size: 1rem;
    line-height: 1.7;
    color: var(--text);
    margin-bottom: 1rem;
  }

  .sr-badges {
    display: flex;
    flex-wrap: wrap;
    gap: 1.5rem;
    margin-top: 1.5rem;
  }

  .sr-badge {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.85rem;
    color: var(--muted);
  }

  .sr-badge svg {
    color: var(--accent);
    flex-shrink: 0;
  }

  .screenshot-placeholder {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    aspect-ratio: 16 / 10;
    border: 1px dashed rgba(212, 145, 94, 0.3);
    border-radius: 12px;
    background: linear-gradient(135deg, rgba(212, 145, 94, 0.03), rgba(212, 145, 94, 0.01));
    color: var(--muted);
    font-size: 0.75rem;
    font-family: var(--font-mono);
    opacity: 0.6;
  }

  @media (max-width: 768px) {
    .sr-grid {
      grid-template-columns: 1fr;
    }

    .sr-right {
      order: -1;
    }
  }
</style>
