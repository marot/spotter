services:
  dolt:
    image: dolthub/dolt-sql-server:1.5.0
    ports:
      - "13306:3306"
    volumes:
      - spotter_dolt_data:/var/lib/dolt
      - ./docker/dolt-init.sql:/docker-entrypoint-initdb.d/01-init.sql
    environment:
      DOLT_ROOT_PASSWORD: spotter

volumes:
  spotter_dolt_data:
